apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gpu-telemetry.fullname" . }}-streamer
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: streamer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: streamer
    spec:
      containers:
      - name: streamer
        image: "{{ .Values.global.imagePrefix }}/streamer:latest"
        imagePullPolicy: IfNotPresent
